$('#myModal').modal('hide');

document.getElementById('avatar-image-sm').src = "<%= url_for(@user.user_profile.avatar) %>"
document.getElementById('avatar-image-lg').src = "<%= url_for(@user.user_profile.avatar) %>"
document.getElementById('avatar-image-modal').src = "<%= url_for(@user.user_profile.avatar) %>"

$.bootstrapGrowl("Avatar atualizado com sucesso!", {
  type: 'success', // (null, 'info', 'error', 'success')
  align: 'right', // ('left', 'right', or 'center')
  allow_dismiss: true,
  stackup_spacing: 10 // spacing between consecutively stacked growls.
});
